version: "3.9"

services:
  web:
    build: .
    expose:
      - "3000"
    environment:
      - NODE_ENV=production

  nginx:
    image: nginx:stable-alpine
    depends_on:
      - web
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
